const initState = {
  projects: [
    {id: '1', title: 'Education for kids'},
    {id: '2', title: 'Honey Farms'}
  ]
}

const projectReducer = (state = initState, action) => {
  return state;
}

export default projectReducer;
